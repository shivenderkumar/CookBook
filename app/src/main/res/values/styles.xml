<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="dialog_slide_animation">
        <item name="android:windowEnterAnimation">@anim/dialogstart</item>
        <item name="android:windowExitAnimation">@anim/dialogend</item>
    </style>

    <style name="AppTheme.Toolbar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:editTextColor">@color/gray_text</item>
        <item name="hintTextColor">@color/gray_text</item>
    </style>

    <style name="AppTheme.SearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@color/gray_text</item>
    </style>

</resources>